# Copy this file to .env and fill in your actual key.
# .env is git-ignored — keys never leave your machine.

# Get one at https://console.groq.com (free, no credit card needed)
GROQ_API_KEY=

# Optional: override the proxy port (default 3334)
# CLAUDE_PROXY_PORT=3334
